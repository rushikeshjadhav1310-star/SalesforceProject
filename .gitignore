# Ignore package lock files (optional)
package-lock.json
yarn.lock
# Ignore Salesforce-specific files (if applicable)
.sfdx/

# Keep empty folders (preserve structure)
# Add a .gitkeep file manually in empty folders you want to track
!.gitkeep